---
title: HTML & CSS Basico 👶🏼
description: Aprenderemos conceptos basicos de HTML $ CSS
date: 2019-05-06T14:18:45.798Z
lang: es
published: false
tags: html, css, basico
---

# Objetivos

- Aprenderemos los conceptos basicos de la semantica `HTML`.
- Aprenderemos sobre `a11y` y porque es importante.
- Aprenderemos sobre `SEO` y ¿Cómo podría usarse correctamente en `HTML` para mejorarlo?.
- Aprenderemos sobre el modelo de caja en `CSS`.
- Aprenderemos sobre el tipo de `posicionamiento` que existe.
- Aprenderemos a como usar `tipografias` y controlarla.

porque `a11y` = Accessibility
`a@@@@@@@y` = empeiza con `a` tiene 11 caracteres y termina con `y`

- el `14em` = `em` es una unidad relativa con respecto al tamano de fuente `font-size: 16px`, siendo claros con la letra `M`

```html
.nav { position: sticky; }
```

cuando no hace scroll funciona como `position: relative` cuando hace scroll y se cumple su margin empieza a funcionar como `position: fixed` 🤯

- font face : es para cargar tu propia fuente en tu CSS
  inlcusi puedes buscar la fuente de otro navegador y usarlo.
  https://cybmeta.com/font-face-como-utilizar-cualquier-fuentetipografia-en-tu-web

[font-face](https://alligator.io/css/font-face)

MEMes - css is awesomo
